#!/bin/sh
set -eu

git fetch --quiet

git submodule foreach --quiet '\
  cd "$toplevel" \
  && git diff origin/stable...HEAD --submodule=log -- "$path" \
     | sed -e "s/Submodule //" \
     | grep -s "." \
     && echo "" \
  ; cd "$OLDPWD"\
'
